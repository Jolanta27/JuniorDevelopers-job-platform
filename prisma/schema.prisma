generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model JobPosting {
  id String @id @default(uuid())
  title String
  location String
  companyName String
  salary Int
  employmentType String?
  description String
}
